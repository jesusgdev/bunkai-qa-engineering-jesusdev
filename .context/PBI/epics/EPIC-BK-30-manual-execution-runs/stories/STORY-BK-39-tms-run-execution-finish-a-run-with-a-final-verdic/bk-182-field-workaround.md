> [!SUCCESS]
> {status:green|PARTIAL WORKAROUND} Manual QA can create the Run through cookie-session UI/API flow, then use Bearer to validate finish behavior on the existing Run. This does not unblock AI/CI/PAT Run creation.
